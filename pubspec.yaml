name: optional
description: A wrapper for optional values forwarding to functions.
version: 1.0.0
repository: https://github.com/RightBrainPro/optional

publish_to: http://rightbrain.pro

environment:
  sdk: ^3.4.0

dependencies: null

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.7
